
const CurrentApp = () => {
    // const user = useAppSelector(state => state.auth.user);
    // const role = user?.roleName;
    return (
        <div className={'text-gray-500 capitalize'}>
            {/*{role === 'admin' && `Admin`}*/}
            {/*{role === 'organisation' && `${user?.organisation?.name} `}*/}
        </div>
    )
}

export default CurrentApp
